# Тема 3. Операторы, условия, циклы

Отчет по теме #3 выполнен:

- Кочурин Никита Сергеевич
- ИВТ-23-2

| Задание | Лаб_раб | Сам_раб |
| ---------- | ------- | ------- |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |         |
| Задание 7 | + |         |
| Задание 8 | + |         |
| Задание 9 | + |         |
| Задание 10 | + |         |

знак «+» — задание выполнено; знак «–» — задание не выполнено;

Работу проверили:

- кандидат экономических наук, доцент Панов М. А.

## Лабораторная работа №1

### Создайте две переменные, значения которых будете вводить через консоль. Также составьте условие, в котором будут сравниваться созданные ранее переменные. Если условие выполняется, выведите в консоль «Выполняется», если нет — «Не выполняется».

``питон 
one = int(input('Введите значение первой переменной: ')) 
два = int(input('Введите значение второй переменной: ')) 
если одно >= два: 
    print("Выполняется") 
ещё: 
    print("Не выполняется")
```

### Результат

<img width="762" height="607" alt="изображение" src="https://github.com/user-attachments/assets/65e70e11-c374-4a61-9460-3bfc7e64ca03" />


### Выводы

В этом коде созданным переменным присваиваются значения, введённые с консоли. 
С помощью оператора `if` переменные сравниваются между собой
.1 Если переменная one больше или равна переменной two, выводится «Выполняется»
.2 В противном случае выводится сообщение «Не выполняется»

---
## Лабораторная работа № 2

### Напишите программу, которая будет определять значения переменной, меньшие 0, большие 0 и меньшие 10 или большие 10. Это нужно реализовать с помощью одной переменной, значение которой будет вводиться через консоль, а также с помощью конструкций `if`, `elif`, `else`

``питон
one = int(input('Введите значение первой переменной: ')) 
если один < 0: 
    print("Переменная меньше 0") 
< 0 elif один < 10: 
    print("Переменная больше 0 и меньше 10") 
ещё: 
    print("Переменная больше 10")
```

### Результат

<img width="758" height="609" alt="изображение" src="https://github.com/user-attachments/assets/e9a5f755-49b7-4a7e-8567-d5bee35cf5b9" />


### Выводы

Задача решается с помощью условных конструкций `if`, `elif` и `else`. В Python реализована удобная проверка диапазонов с помощью конструкции `start < value < end`, которая также используется при решении задачи

---
## Лабораторная работа № 3

### Напишите программу, которая будет проверять, есть ли переменная в указанном массиве, с помощью логического оператора `in`. Самостоятельно посмотрите, как программа работает со значениями, которых нет в массиве `числа`.

```питон
числа = [1, 3, 4, 6, 8, 9] 
значение = int(input('Введите значение переменной: ')) 
если значение в числах: 
    print('Переменная есть в этом массиве') 
ещё: 
    print('Переменной нет в этом массиве')
```

### Результат

<img width="758" height="606" alt="изображение" src="https://github.com/user-attachments/assets/228175f3-5344-49fc-b079-19b021556bdf" />


### Выводы 

Логический оператор `in` проверяет наличие элемента в некоторой структуре данных и возвращает `True`, если элемент существует, и `False`, если его нет. Используя это свойство в сочетании с условными операторами, мы можем написать программу для проверки наличия переменной в массиве

---
## Лабораторная работа № 4

### Напишите программу, которая будет определять, находится ли переменная в указанном массиве, и если да, то проверять, чётная она или нет. Самостоятельно протестируйте эту программу с разными значениями переменной `значение`.

``питон
числа = [1, 3, 4, 6, 8, 9, 15, 16, 73, 321, 322] 
значение = int(input('Введите значение переменной: ')) 
если значение в числах: 
    если значение % 2 == 0: 
        print('Переменная четная и есть в массиве numbers') 
    ещё: 
        print('Переменная нечётная и есть в массиве numbers') 
ещё: 
    print(f'Переменной нет в массиве numbers, и она равна {value}')
```
### Результат

<img width="760" height="606" alt="изображение" src="https://github.com/user-attachments/assets/ac92f84f-ef0a-410a-9c5c-988e09ea7e8c" />


### Выводы 
Эта программа является усложнённой версией предыдущей. В ней добавлен дополнительный блок проверки переменной на чётность. Это происходит с помощью математического оператора `%`, который возвращает остаток от деления числа a на число b

---
## Лабораторная работа № 5

### Напишите программу, в которой с помощью цикла `for` значения переменной `i` будут меняться от 0 до 10, и посмотрите, как в цикле работают различные виды сравнений и операций.

```питон
for i in range(10): 
    print('i = ', i) 
    если i равно 0: 
 i += 2  
    если i равно 1: 
        продолжать  
    если i равно 2 или i равно 3: 
        print('Переменная равна 2 или 3') 
    elif i in [4, 5, 6]: 
        print('Переменная равна 4, 5 или 6') 
    ещё: 
        перерыв
```
### Результат

<img width="758" height="844" alt="изображение" src="https://github.com/user-attachments/assets/b27b1a6d-1565-456b-a63d-04e31ef96d73" />


### Выводы 

Переменная в цикле проходит по списку от 0 до 10 не включительно. Этот список — результат работы функции `range()`. Когда условия внутри массива возвращают `True`, переменная изменяется определённым образом. Оператор `continue` пропускает текущую итерацию и переходит к следующей, `break` — останавливает выполнение цикла

---
## Лабораторная работа № 6

### Напишите программу, в которой с помощью цикла `for` определяется, есть ли переменная `value` в строке `string`, и посмотрите, как работает оператор else для циклов. Самостоятельно посмотрите, что выведет программа, если значение переменной `value` окажется в строке `string`.

```питон
строка = 'Привет всем, кто изучает Python!'  
значение = ввод() 
для i в строке: 
    если i равно значению: 
 индекс = строка.найти(значение) 
        print(f'Буква "{value}" есть в строке под {index} индексом') 
        перерыв  
ещё: 
    print(f'Буквы "{value}" нет в указанной строке')
```

### Результат

<img width="731" height="558" alt="изображение" src="https://github.com/user-attachments/assets/3a80b41e-6633-4a7c-b8a9-4c2bd45e1586" />


### Выводы 

В этом скрипте цикл `for` проходит по всем символам строки `string` . `Если string[i]` равно введенному пользователем значению `value`, то программа возвращает индекс первого вхождения `value` в `string`. Если же такого значения нет, то программа выводит информацию об отсутствии `value` в `string`.

---

## Лабораторная работа № 7

### Напишите программу, в которой вы наглядно увидите, как работает цикл `for` в обратном порядке, то есть, например, не от 0 до 10, а от 10 до 0. В уже готовой программе показано вычитание из 100, а вам во время реализации программы нужно будет придумать свой вариант применения обратного цикла.

```питон
строка = "шытревереп"  
for i in range(len(string) -1 , -1, -1): 
    print(строка[i], конец='')
```

### Результат

<img width="726" height="335" alt="изображение" src="https://github.com/user-attachments/assets/4dd79a83-851f-44ca-9d62-0be7a9e1942b" />


### Выводы 

В моей реализации `i` в `for` принимает значения в диапазоне от длины строки `string` до 0 включительно с шагом -1. `i` используется как индекс для вывода символов строки `string` от последнего к первому. Таким образом, слово печатается задом наперёд.

---

## Лабораторная работа №8

### Напишите программу с использованием цикла `while`, внутри которого есть какие-либо проверки, но будьте осторожны, поскольку циклы `while` при неправильно написанных условиях могут стать бесконечными, как показано в примере.

```питон
значение = 50  

пока значение > 0: 
    если значение % 2 == 0: 
        печать(значение) 
        если значение % 10 равно 0: 
            print(f'######{value // 10}#######') 
    если значение < 0: 
        перерыв  
 значение -= 1
```

### Результат

<img width="727" height="1063" alt="изображение" src="https://github.com/user-attachments/assets/89c9beea-1cef-475c-91e4-2f04f47cd0f8" />


### Выводы 
Цикл повторяется до тех пор, пока значение `value` больше 0. С каждой итерацией `value` уменьшается на 1. Если `value` чётное, оно выводится на экран. Если `value` без остатка делится на 10, результат `value // 10` выводится в консоль.

---

## Лабораторная работа №9

### Напишите программу с использованием вложенных циклов и одной проверкой внутри них. Самое главное — не забывайте, что при использовании вложенных циклов нельзя использовать одинаковые имена итерируемых переменных

```питон
значение = 0  
for i in range(10): 
    for j in range(10): 
        если i не равно j: 
 значение += j 
        ещё: 
            пройти  
печать(значение)
```

### Результат

<img width="729" height="403" alt="изображение" src="https://github.com/user-attachments/assets/bd455513-6589-4932-ae2f-5577ab004e1a" />


### Выводы 

В этом скрипте используются вложенные циклы. На каждую итерацию внешнего цикла приходится десять итераций внутреннего. При этом внутренний цикл может взаимодействовать с переменной внешнего цикла. Так, например, `i` сравнивается с `j` в рамках данной задачи.

---

## Лабораторная работа № 10

### Напишите программу с использованием `flag`, которая будет определять, есть ли в массиве нечётное число. В данной задаче `flag` выступает в роли индикатора наличия нечётного числа в исходном массиве четных чисел.

```питон
even_array = [2, 4, 6, 8, 9] 
флаг = False  
для каждого из even_array: 
    если значение % 2 == 1: 
 флаг = True  
если флаг равен True: 
    print('В массиве есть нечётное число') 
ещё: 
    print('В массиве все числа чётные')
```
### Результат

<img width="727" height="411" alt="изображение" src="https://github.com/user-attachments/assets/a2b7309c-fde6-4097-a61d-48c758cc45fd" />


### Выводы 

flag-и используются в качестве индикатора при проверке некоторого условия. В приведенном коде `flag` принимает исходное значение `False`. С помощью цикла `for` элементы `even_array` проверяются на четность, и в случае, если один из элементов не проходит проверку на четность, `flag = True`. После завершения проверки мы используем `flag`, чтобы вывести в консоль информацию о наличии или отсутствии в массиве четных элементов 

---

## Самостоятельная работа №1

### Напишите программу, которая преобразует 1 в 31. Для выполнения поставленной задачи необходимо обязательно и только один раз использовать: Цикл `for`; *= 5; += 1. Никаких других действий или циклов использовать нельзя.

```питон
число = 1  
for i in range (2): 
 num *= 5  
 num += 1  
печать(число)
```

### Результат

<img width="466" height="409" alt="изображение" src="https://github.com/user-attachments/assets/19273716-f311-4f24-af00-5383dc06bb9f" />


### Выводы

В этом коде с помощью цикла `for` и математических операций реализуется следующая последовательность:
-  (1 \* 5 + 1) \* 5 + 1 (31) 

---
## Самостоятельная работа № 2
### Напишите программу, которая выводит фразу «Hello World» в обратном порядке, при этом каждая буква находится в отдельной строке консоли. При этом необходимо обязательно использовать любой цикл, а программа должна занимать не более 3 строк в редакторе кода.

```питон
строка = 'Привет, мир!'  
for i in range(len(line)-1, -1, -1): 
    print(строка[i])
```

### Результат

<img width="469" height="560" alt="изображение" src="https://github.com/user-attachments/assets/286c0c83-8efd-4bd7-a286-9720ac1c0df1" />


### Выводы 

Поскольку со стоками в Python можно работать так же, как со списками, для решения задачи можно пройтись по строке от последнего элемента к нулевому.

---
## Самостоятельная работа № 3

### На вход подается значение в диапазоне от 0 до 10 включительно (необходимо учесть в программе). Если введенное число не соответствует требованиям, необходимо вывести оповещение об этом в консоль и остановить программу. Код должен вычислять, в каком диапазоне находится полученное число. Необходимо учитывать три диапазона: • от 0 до 3 включительно; • от 3 до 6; • от 6 до 10 включительно. Результатом работы программы будет выведенный в консоль диапазон. Программа должна занимать не более 10 строк в редакторе кода.

```питон
userIn = int(input("Введите число: ")) 
если пользователь вошёл в диапазон (11): 
    если пользователь ввел <= 3: вывести('от 0 до 3 включительно') 
    elif пользователь ввел < 6: print('от 3 до 6;') 
    else: print('от 6 до 10 включительно.') 
else: print("Число не подходит!")
```

### Результат

<img width="466" height="556" alt="изображение" src="https://github.com/user-attachments/assets/ce715cf4-d6d5-463f-8426-a6b11eeb6dcf" />


### Выводы 

С помощью нескольких конструкций `if-else` и `elif` мы обрабатываем число в соответствии с заданными параметрами. Также предусмотрен случай, когда число не попадает в обрабатываемый диапазон

---
## Самостоятельная работа № 4

### Напишите программу на Python, которая принимает от пользователя предложение (на английском языке) в качестве входных данных. Выполните следующие операции и выведите результаты: 1. Выведите длину предложения. 2. Переведите предложение в нижний регистр. 3. Подсчитайте количество гласных (a, e, i, o, u) в предложении. 4. Замените все слова «ugly» на «beauty». 5. Проверьте, начинается ли предложение со слова «The» и заканчивается ли оно словом «end».  Проверьте работу программы как минимум на трёх предложениях, чтобы убедиться, что все условия соблюдены

```питон
line_info def(строка): 
 символы = ['a', 'e', 'i', 'o', 'u'] 

    print(f'Исходная строка — {line}') 
    print(f'Длина предложения: {len(line)}') 
 строка = str.lower(строка) 
    печать(строка) 

 счётчик = 0  
    для каждого в строке: 
        если символ входит в набор символов: counter += 1  
    print(f'Количество гласных: {counter}') 

 строка = str.replace(строка, 'уродливый', 'красивый') 
    печать(строка) 

    если строка[:3] == 'the' или строка [-3:] == 'end': 
        print('Предложение начинается с "The" или заканчивается на "end".') 
    print("####################################################") 

line_info("SoMe LinE") 
line_info("Уродливый мир") 
line_info("aeiou конец")
```

### Результат

<img width="766" height="1163" alt="изображение" src="https://github.com/user-attachments/assets/8d08b9d1-67f5-46ba-b04c-c40c231fd302" />


## Выводы

Для решения поставленной задачи я создал функцию, которая позволяет выполнить всю проверку за один запуск без дублирования кода. Задачи реализованы с помощью методов `str` и возможности работать со строкой как со списком.

---

## Самостоятельная работа № 5

### Составить программу из фрагментов кода 
<img width="340" height="530" alt="изображение" src="https://github.com/user-attachments/assets/e52a7c3c-9bec-47c5-8baa-458ab473c151" />


```питон
значения = [0, 2, 4, 6, 8, 10] 

счётчик = 0  
пока счётчик не равен 10: 
 строка = 'привет'  
    если счётчик в значениях: 
 строка = строка + ' мир'  
    печать(строка) 
 счётчик+=1  

память = ' мир'  
print(строка+память)
```

### Результат

<img width="562" height="739" alt="изображение" src="https://github.com/user-attachments/assets/0b1bab96-4520-40de-85b5-47ed78b7aca8" />


### Выводы 

Расположив строки таким образом, мы получаем результат, соответствующий заданию

---

## Общие выводы по теме

В ходе выполнения лабораторных и самостоятельных работ я поработал с операторами, условиями и циклами в языке Python. Из теоретической части я узнал об основных концепциях языка, преимуществах и недостатках вложенных циклов, а также о том, как выполняются побитовые операции. С помощью лабораторных и самостоятельных работ я закрепил полученные знания.
